---
import Layout from '../layouts/Layout.astro';
import { Image } from 'astro:assets';
import portrait from '../assets/gallery/portrait.jpg';
import wedding from '../assets/story/wedding.jpg';
import plane from '../assets/story/plane.jpg';
import skylarBorn from '../assets/story/skylar-born.jpg';
import podcast from '../assets/story/podcast.jpg';
import richterEra from '../assets/story/richter-era.jpg';
import havenBorn from '../assets/story/haven-born.jpg';
import clearwater from '../assets/story/clearwater.jpg';
import bootcamp from '../assets/story/10x-bootcamp.jpg';
import southAfrica from '../assets/story/south-africa.jpg';
import seminar from '../assets/story/seminar.jpg';
import g20Summit from '../assets/story/g20-summit.jpg';
import presentDay from '../assets/story/present-day.jpg';
---

<Layout title="About — Devin Alexander" description="The full story. Where I started, what shaped my perspective, and what I believe about building things that matter." image="/og/about.png">
  <main>
    <section class="about-page">
      <div class="about-inner">

        <!-- ═══════════ HERO ═══════════ -->
        <div class="about-hero">
          <div class="hero-image-wrap">
            <div class="hero-image-frame">
              <Image src={portrait} alt="Devin Alexander" widths={[400, 800]} format="webp" quality={85} loading="eager" class="hero-portrait" />
            </div>
          </div>
          <span class="hero-tag">About</span>
          <h1 class="hero-name">Devin Alexander</h1>
          <p class="hero-identity">Ten years of marketing work, in four countries, across thirteen industries. Here's how I got here.</p>
          <div class="hero-descriptors">
            <span class="desc teal">Marketing</span>
            <span class="desc-dot" aria-hidden="true">&middot;</span>
            <span class="desc amber">Operations</span>
            <span class="desc-dot" aria-hidden="true">&middot;</span>
            <span class="desc rust">Design & Dev</span>
            <span class="desc-dot" aria-hidden="true">&middot;</span>
            <span class="desc gold">GTM</span>
          </div>
        </div>

        <!-- ═══════════ STORY — PHOTO ESSAY ═══════════ -->
        <div class="story">
          <div class="story-tag"><span class="tag-number">01</span><span class="tag-dash" aria-hidden="true"></span><span class="tag-label">Story</span></div>

          <!-- ──── Chapter 1: Where It Started ──── -->
          <div class="chapter">
            <div class="chapter-marker"><span class="marker-year">2016–2017</span><span class="marker-dash" aria-hidden="true"></span><span class="marker-label">Where It Started</span></div>

            <figure class="photo-full">
              <Image src={wedding} alt="Devin and Georgia's wedding, 2017" widths={[600, 960, 1200]} format="webp" quality={82} loading="lazy" class="story-img" />
              <figcaption>Georgia and I, 2017. Twenty years old and certain about exactly one thing.</figcaption>
            </figure>

            <p class="story-text story-lead">
              <span class="drop-cap">M</span>y career in marketing started in July of 2016. I was a junior advertiser at Flood Media. Allen, one of their Google Ads specialists, taught me the fundamentals and sent me on my way. That systematic, "if this, then that" logic became the foundation I'd build everything else on top of. Input, output, measure, repeat. <span class="story-accent">I didn't know it yet, but I was teaching myself how to think in leverage.</span> Small inputs, outsized outputs.
            </p>
            <p class="story-text">
              Then Georgia. We'd been together since we were teenagers, and we married in 2017. Twenty years old and certain about exactly one thing. Shortly after the wedding, I got on a plane to England — Georgia's home — and stayed. No safety net. No five-year plan. Just the conviction that <span class="story-accent">the next version of my life was somewhere I hadn't been yet.</span>
            </p>

            <figure class="photo-inline-small">
              <Image src={plane} alt="View from a plane window above the clouds" widths={[280, 500]} format="webp" quality={80} loading="lazy" class="story-img" />
              <figcaption>Somewhere over the Atlantic.</figcaption>
            </figure>
          </div>

          <hr class="chapter-divider" aria-hidden="true" />

          <!-- ──── Chapter 2: Atlanta ──── -->
          <div class="chapter">
            <div class="chapter-marker"><span class="marker-year">2018–2019</span><span class="marker-dash" aria-hidden="true"></span><span class="marker-label">Atlanta</span></div>

            <div class="photo-pair">
              <figure>
                <Image src={skylarBorn} alt="Skylar as a newborn, July 2018" widths={[400, 700]} format="webp" quality={82} loading="lazy" class="story-img" />
                <figcaption>Skylar, July 2018.</figcaption>
              </figure>
              <figure>
                <Image src={podcast} alt="Devin recording The Essentials podcast" widths={[400, 700]} format="webp" quality={82} loading="lazy" class="story-img" />
                <figcaption>The version of me trying to figure it all out.</figcaption>
              </figure>
            </div>

            <p class="story-text">
              In September 2018, I became Creative Director at The Customer Factory, a chiropractic marketing company serving over a hundred practices. We'd moved to Atlanta, and I was learning fast. Marketing isn't just "if this, then that." There are far more variables in strategic customer acquisition than any textbook prepares you for.
            </p>
            <p class="story-text">
              Skylar was born that July. <span class="story-accent">Suddenly the stakes weren't abstract anymore. Every decision had weight.</span>
            </p>
            <p class="story-text">
              By January 2019, they promoted me to VP. I was responsible for marketing The Customer Factory itself, to chiropractors. I built lead magnets that attracted hundreds of doctors a week to our email lists, cultivated referral relationships with clients who sent new business our way, and activated referral sources like chiropractic consultants. We hit the highest new-business numbers the company had ever seen. I also launched "The Essentials," a podcast where I broke down marketing fundamentals. Not to build an audience. To force myself to articulate what I was learning.
            </p>

            <blockquote class="pull-quote">Marketing isn't formulas. It's people. The data tells you what happened. The people tell you why.</blockquote>
          </div>

          <hr class="chapter-divider" aria-hidden="true" />

          <!-- ──── Chapter 3: Fortune 500s ──── -->
          <div class="chapter">
            <div class="chapter-marker"><span class="marker-year">2019–2021</span><span class="marker-dash" aria-hidden="true"></span><span class="marker-label">Fortune 500s</span></div>

            <figure class="photo-feature">
              <Image src={richterEra} alt="Devin during the Richter years" widths={[400, 600]} format="webp" quality={82} loading="lazy" class="story-img" />
              <figcaption>The Richter years. Learning that ambition and humility aren't opposites.</figcaption>
            </figure>

            <p class="story-text">
              In August 2019, I had a phone call with Robert Cornish, CEO and Founder of Richter. A media company that produced videos and sales strategies for some of the largest companies in the world. Over <span class="story-stat">40 of the Fortune 500</span> on their client roster. I pitched myself for the VP of Marketing role. He accepted.
            </p>
            <p class="story-text">
              It was some of the most challenging, exciting work I'd ever done. We launched thought leadership forums, podcasts, enterprise positioning campaigns. I was touching almost every part of the marketing lifecycle. The jump from scrappy startup to enterprise was disorienting at first. Bigger budgets, longer timelines, thicker politics. <span class="story-accent">But the fundamentals were the same: find the leverage, tell the story, move the number.</span>
            </p>

            <blockquote class="pull-quote">Scale doesn't simplify decisions. It amplifies them.</blockquote>

            <p class="story-text">
              Robert then launched ContentOne, a subsidiary targeting B2B SMBs. I was tapped as VP of Marketing & Operations. Running our own promotion, managing the production team for client projects. This is where automation became a core part of my workflow. Dropbox, DocuSign, Monday.com, everything scattered everywhere. So we streamlined. Automated. Built our own production monitoring systems, added Zapier, and increased efficiency dramatically.
            </p>
          </div>

          <hr class="chapter-divider" aria-hidden="true" />

          <!-- ──── Chapter 4: Two Daughters, One Startup ──── -->
          <div class="chapter">
            <div class="chapter-marker"><span class="marker-year">2022</span><span class="marker-dash" aria-hidden="true"></span><span class="marker-label">Two Daughters, One Startup</span></div>

            <figure class="photo-feature">
              <Image src={havenBorn} alt="Haven as a newborn, March 2022" widths={[400, 600]} format="webp" quality={82} loading="lazy" class="story-img" />
              <figcaption>Haven, March 2022.</figcaption>
            </figure>

            <p class="story-text">
              Haven arrived in March 2022. Two daughters under four. And I decided that was the perfect time to take a leap.
            </p>
            <p class="story-text">
              Jeremy, the CEO of B2B Business Experts, reached out. He had one employee, a salesperson, and was handling delivery himself. He specialized in B2B marketing and appointment setting for insurance agents. At first I hesitated. Startup life. "What if it crashes and burns?" I'd just had a baby. That was a lot to think about. But I went with my gut and took the job as his head of marketing.
            </p>
            <p class="story-text">
              This role taught me more about data analysis, process documentation, and process optimization than anything else I'd done. It was all about streamlining systems to the absolute limit so we could be wildly profitable while still delivering services worth paying for. We went from <span class="story-stat">$30,000 a month</span> in revenue when I started to <span class="story-stat">$300,000 a month</span> by the time I departed.
            </p>

            <blockquote class="pull-quote">Comfort makes me restless. Pressure makes me precise.</blockquote>

            <figure class="photo-feature">
              <Image src={clearwater} alt="Devin and daughter at sunset in Clearwater, Florida" widths={[400, 600]} format="webp" quality={82} loading="lazy" class="story-img" />
              <figcaption>Clearwater, Florida. The rare pause.</figcaption>
            </figure>
          </div>

          <hr class="chapter-divider" aria-hidden="true" />

          <!-- ──── Chapter 5: Never Stop ──── -->
          <div class="chapter">
            <div class="chapter-marker"><span class="marker-year">2023</span><span class="marker-dash" aria-hidden="true"></span><span class="marker-label">Never Stop</span></div>

            <figure class="photo-wide">
              <Image src={bootcamp} alt="10x Bootcamp event in Miami" widths={[500, 800]} format="webp" quality={82} loading="lazy" class="story-img" />
              <figcaption>10x Bootcamp, Miami. Still a student.</figcaption>
            </figure>

            <p class="story-text">
              2023 was depth over breadth. The insurance marketing pivot continued. A training platform built from scratch. The team grew to six. I was deep in data, in optimization, in the mechanics of turning attention into action. But the real growth that year wasn't the metrics. It was the discipline. <span class="story-accent">I stopped chasing novelty and started mastering repetition.</span>
            </p>

            <blockquote class="pull-quote">The growth that year wasn't in the metrics. It was in the discipline.</blockquote>
          </div>

          <hr class="chapter-divider" aria-hidden="true" />

          <!-- ──── Chapter 6: Home ──── -->
          <div class="chapter">
            <div class="chapter-marker"><span class="marker-year">2024 – Present</span><span class="marker-dash" aria-hidden="true"></span><span class="marker-label">Home</span></div>

            <figure class="photo-full">
              <Image src={southAfrica} alt="Devin with both daughters in South Africa" widths={[600, 960, 1200]} format="webp" quality={85} loading="lazy" class="story-img" />
              <figcaption>South Africa, 2024.</figcaption>
            </figure>

            <p class="story-text">
              In May 2024, I started freelance consulting and we relocated to South Africa. The plan was to build a portfolio across niches and revenue levels — chiropractic, insurance, B2B, e-commerce — and figure out what actually worked across them. The takeaway: not much. Every business has its own gravity. The funnel-and-ads playbook works in a handful of cases and breaks in the rest. So I stopped pitching a system and <span class="story-accent">started approaching every engagement like a bespoke suit — measured, fitted, then refined.</span>
            </p>

            <div class="photo-pair">
              <figure>
                <Image src={seminar} alt="Devin speaking at a seminar" widths={[400, 700]} format="webp" quality={82} loading="lazy" class="story-img" />
                <figcaption>Teaching what took a decade to learn.</figcaption>
              </figure>
              <figure>
                <Image src={g20Summit} alt="Devin at the IF20 forum at the G20" widths={[400, 700]} format="webp" quality={82} loading="lazy" class="story-img" />
                <figcaption>IF20 at the G20.</figcaption>
              </figure>
            </div>

            <p class="story-text">
              In November 2025, I was offered the role of CMO at Bitmern Mining, owned by Giannis Andreou. Within a month, Giannis promoted me to COO of Andreou Enterprises. I served as his second-in-command, running marketing across the brand portfolio. That role taught me something vital: <span class="story-accent">work hard, be customer-focused, ask them what they want instead of assuming, and never pretend to know it all.</span> Now I'm Fractional CMO (contract) at Jensen Business Advisory, leading growth for David Lee Jensen, a Fortune Top 50 speaker and bestselling author, and his 728 Network, an exclusive mastermind helping entrepreneurs scale past $1M.
            </p>
            <p class="story-text">
              Five years ago I also picked up software development. Mostly front-end. TypeScript, Ruby, Tailwind, Bun, Node. All in service of being able to build bespoke systems to match whatever the strategy demands, or lead a team who can because I understand the systems too.
            </p>
            <p class="story-text">
              I'm preparing to return to Clearwater soon. Still actively working in marketing, still building on the side, still being a dad and a husband. <span class="story-accent">That last part is the whole point.</span>
            </p>

            <figure class="photo-feature photo-closer">
              <Image src={presentDay} alt="Devin with daughter, black and white" widths={[400, 600]} format="webp" quality={85} loading="lazy" class="story-img" />
              <figcaption>The whole point.</figcaption>
            </figure>
          </div>
        </div>

        <!-- ═══════════ SECTION BREAK ═══════════ -->
        <div class="section-break" aria-hidden="true">
          <span class="break-dot"></span>
          <span class="break-dot"></span>
          <span class="break-dot"></span>
        </div>

        <!-- ═══════════ HOW I WORK ═══════════ -->
        <div class="convictions">
          <div class="convictions-tag"><span class="tag-number">02</span><span class="tag-dash" aria-hidden="true"></span><span class="tag-label">Method</span></div>
          <h2 class="convictions-heading">How I work.</h2>

          <div class="conviction-grid">
            <article class="conviction">
              <span class="conviction-number">01</span>
              <h3 class="conviction-title">The leverage is usually in the boring part.</h3>
              <p class="conviction-text">When a client asks me to fix their funnel, I run the funnel — but I also walk the process behind it. Nine times out of ten the constraint isn't the landing page. It's a handoff someone accepted as a constant five years ago. Find that, and the funnel fixes itself.</p>
            </article>
            <article class="conviction">
              <span class="conviction-number">02</span>
              <h3 class="conviction-title">I work across disciplines because I couldn't afford specialists.</h3>
              <p class="conviction-text">Marketing, operations, design, and code aren't four jobs to me. They're four angles on the same work. I started learning them out of necessity at a junior salary, and I kept learning them because the seams between them are where most teams lose money.</p>
            </article>
            <article class="conviction">
              <span class="conviction-number">03</span>
              <h3 class="conviction-title">I'd rather build a system than be a hero.</h3>
              <p class="conviction-text">The $30K→$300K/month run at B2B Business Experts wasn't won by a campaign. It was won by killing the parts of the process that needed me and replacing them with parts that didn't. A team that doesn't need me at 2am is worth more than any sprint I could pull.</p>
            </article>
            <article class="conviction">
              <span class="conviction-number">04</span>
              <h3 class="conviction-title">Price for impact, not for hours.</h3>
              <p class="conviction-text">Hourly engagements get you a deck. Retainers priced for actual lift get you a partner who's allowed to be wrong, fix it, and keep going. I only take the second kind, and I turn away the first regardless of the rate.</p>
            </article>
          </div>
        </div>

        <!-- ═══════════ AREAS OF FOCUS ═══════════ -->
        <div class="skills">
          <div class="skills-tag"><span class="tag-number">03</span><span class="tag-dash" aria-hidden="true"></span><span class="tag-label">Focus</span></div>
          <h2 class="skills-heading">Areas of focus.</h2>
          <div class="skills-grid">
            <div class="skill-group">
              <h3 class="group-label">Marketing strategy & execution</h3>
              <p class="group-desc">Positioning, GTM, landing pages, content engines, cold outreach, paid acquisition. Whichever discipline the lever requires — usually two or three at once.</p>
            </div>
            <div class="skill-group">
              <h3 class="group-label">Operations & team leadership</h3>
              <p class="group-desc">Process design, hiring, SOPs, workflow automation across Zapier and Monday, KPI reporting. The work that makes the marketing repeatable without me in the room.</p>
            </div>
            <div class="skill-group">
              <h3 class="group-label">Design & front-end</h3>
              <p class="group-desc">Web rebuilds, design systems, brand identity, editorial templates. I build the systems I design — TypeScript, Astro, Svelte, Tailwind — so the gap between intent and ship is small.</p>
            </div>
            <div class="skill-group">
              <h3 class="group-label">Data & infrastructure</h3>
              <p class="group-desc">Analytics setup, event tracking, attribution models, CMS architecture, API integrations, custom admin dashboards. Marketing without data is faith-based.</p>
            </div>
          </div>
        </div>

        <div class="closing-cta">
          <p class="cta-text">If any of this resonates, <a href="/contact" class="cta-link">let's talk</a>.</p>
        </div>

        <div class="page-footer">
          <a href="/" class="back-link">
            <svg width="14" height="14" viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
              <path d="M13 7H1M6 2L1 7l5 5" />
            </svg>
            Back home
          </a>
        </div>
      </div>
    </section>
  </main>
</Layout>

<script>
  import { animate, inView, stagger } from 'motion';

  const isVtNav = window.__vtNav;
  const sections = document.querySelectorAll('.about-hero, .convictions, .skills, .closing-cta');
  const chapters = document.querySelectorAll('.chapter');
  const storyPhotos = document.querySelectorAll('.chapter figure');
  const pullQuotes = document.querySelectorAll('.pull-quote');
  const convictions = document.querySelectorAll('.conviction');
  const convGrid = document.querySelector('.conviction-grid');

  if (!isVtNav) {
    // Initial opacity:0 is set via CSS (html.js-ready selector) so elements
    // never paint visible.
    sections.forEach((el, i) => {
      inView(el, () => {
        animate(el, { opacity: [0, 1], y: [12, 0] }, {
          duration: 0.4,
          delay: i === 0 ? 0 : 0.05,
          easing: [0.25, 1, 0.5, 1],
        });
      }, { amount: 0.15 });
    });

    const storyTag = document.querySelector('.story-tag');
    if (storyTag) {
      inView(storyTag, () => {
        animate(storyTag, { opacity: [0, 1], y: [12, 0] }, {
          duration: 0.4,
          easing: [0.25, 1, 0.5, 1],
        });
      }, { amount: 0.15 });
    }

    chapters.forEach(ch => {
      inView(ch, () => {
        animate(ch, { opacity: [0, 1], y: [12, 0] }, {
          duration: 0.45,
          easing: [0.25, 1, 0.5, 1],
        });
      }, { amount: 0.08 });
    });

    storyPhotos.forEach(fig => {
      inView(fig, () => {
        animate(fig, { opacity: [0, 1], scale: [1.02, 1] }, {
          duration: 0.5,
          easing: [0.25, 1, 0.5, 1],
        });
      }, { amount: 0.1 });
    });

    pullQuotes.forEach(pq => {
      inView(pq, () => {
        animate(pq, { opacity: [0, 1], y: [8, 0] }, {
          duration: 0.5,
          easing: [0.25, 1, 0.5, 1],
        });
      }, { amount: 0.3 });
    });

    if (convGrid) {
      inView(convGrid, () => {
        animate(convictions, { opacity: [0, 1], y: [12, 0] }, {
          duration: 0.35,
          delay: stagger(0.05),
          easing: [0.25, 1, 0.5, 1],
        });
      }, { amount: 0.2 });
    }
  }

  convictions.forEach(card => {
    card.addEventListener('mouseenter', () => {
      const title = card.querySelector('.conviction-title');
      animate(card, { y: -4 }, { duration: 0.15, easing: [0.34, 1.56, 0.64, 1] });
      if (title) animate(title, { x: 3 }, { duration: 0.15, easing: [0.34, 1.56, 0.64, 1] });
    });
    card.addEventListener('mouseleave', () => {
      const title = card.querySelector('.conviction-title');
      animate(card, { y: 0 }, { duration: 0.25, easing: [0.16, 1, 0.3, 1] });
      if (title) animate(title, { x: 0 }, { duration: 0.25, easing: [0.16, 1, 0.3, 1] });
    });
  });

  const skillGroups = document.querySelectorAll('.skill-group');
  const skillsGrid = document.querySelector('.skills-grid');
  if (!isVtNav) {
    if (skillsGrid) {
      inView(skillsGrid, () => {
        animate(skillGroups, { opacity: [0, 1], y: [12, 0] }, {
          duration: 0.35,
          delay: stagger(0.05),
          easing: [0.25, 1, 0.5, 1],
        });
      }, { amount: 0.2 });
    }
  }
</script>

<style>
  /* Pre-hide elements JS will fade in (Layout.astro adds .js-ready before paint). */
  html.js-ready .about-hero,
  html.js-ready .convictions,
  html.js-ready .skills,
  html.js-ready .closing-cta,
  html.js-ready .story-tag,
  html.js-ready .chapter,
  html.js-ready .chapter figure,
  html.js-ready .pull-quote,
  html.js-ready .conviction,
  html.js-ready .skill-group {
    opacity: 0;
  }

  .about-page {
    min-height: 100dvh;
    padding: var(--space-page-top) var(--space-page-x) var(--space-page-bottom);
    background: var(--color-bg);
  }

  .about-inner {
    max-width: 720px;
    margin: 0 auto;
  }

  /* ---- Section tags (reusable, centered) ---- */
  .story-tag, .convictions-tag, .skills-tag {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.65rem;
    margin-bottom: clamp(1.25rem, 2.5vw, 1.75rem);
  }

  .tag-number {
    font-family: var(--font-display);
    font-size: var(--text-md);
    line-height: 1;
    color: var(--color-accent-teal);
  }

  .convictions-tag .tag-number { color: var(--color-accent-amber); }
  .skills-tag .tag-number { color: var(--color-accent-rust); }

  .tag-dash {
    width: 24px;
    height: 1px;
    background: linear-gradient(90deg, var(--color-accent-teal), transparent);
  }

  .convictions-tag .tag-dash { background: linear-gradient(90deg, var(--color-accent-amber), transparent); }
  .skills-tag .tag-dash { background: linear-gradient(90deg, var(--color-accent-rust), transparent); }

  .tag-label {
    font-family: var(--font-body);
    font-size: var(--text-xs);
    font-weight: var(--weight-medium);
    letter-spacing: var(--tracking-wide);
    text-transform: uppercase;
    color: var(--color-text-muted);
  }

  /* ---- Hero (centered) ---- */
  .about-hero {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    margin-bottom: var(--space-block-lg);
  }

  .hero-image-wrap {
    width: clamp(140px, 22vw, 200px);
    margin-bottom: var(--space-element);
  }

  .hero-image-frame {
    position: relative;
    padding: 4px;
    background: linear-gradient(160deg, var(--color-accent-teal), var(--color-accent), var(--color-accent-amber), var(--color-accent-rust));
  }

  .hero-image-frame :global(img) {
    width: 100%;
    height: auto;
    display: block;
    filter: grayscale(0.3) sepia(0.1) brightness(0.95) contrast(1.05);
    aspect-ratio: 3 / 4;
    object-fit: cover;
  }

  .hero-tag {
    font-family: var(--font-body);
    font-size: var(--text-xs);
    font-weight: var(--weight-medium);
    letter-spacing: var(--tracking-wide);
    text-transform: uppercase;
    color: var(--color-accent-teal);
    display: block;
    margin-bottom: 0.75rem;
  }

  .hero-name {
    font-family: var(--font-display);
    font-size: var(--text-3xl);
    font-weight: var(--weight-regular);
    line-height: 1.05;
    letter-spacing: var(--tracking-tight);
    color: var(--color-text);
    margin: 0 0 0.65rem;
  }

  .hero-identity {
    font-family: var(--font-display);
    font-size: var(--text-md);
    line-height: 1.4;
    color: var(--color-text-muted);
    margin: 0 0 1.25rem;
    max-width: 28em;
  }

  .hero-descriptors {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.5em;
    font-family: var(--font-body);
    font-size: var(--text-xs);
    font-weight: var(--weight-regular);
    letter-spacing: var(--tracking-wide);
    text-transform: uppercase;
  }

  .desc.teal { color: var(--color-accent-teal); }
  .desc.amber { color: var(--color-accent-amber); }
  .desc.rust { color: var(--color-accent-rust); }
  .desc.gold { color: var(--color-accent); }
  .desc-dot { color: var(--color-text-muted); opacity: 0.4; font-size: 1.1em; }

  /* ---- Story ---- */
  .story {
    margin-bottom: var(--space-block-lg);
  }

  /* Prose: LEFT-aligned for readability, centered container gives centered feel */
  .story-text {
    font-family: var(--font-display);
    font-size: var(--text-lg);
    line-height: 1.55;
    color: var(--color-text-muted);
    margin: 0 0 1.5rem;
  }

  .story-text:last-child {
    margin-bottom: 0;
  }

  .story-accent {
    color: var(--color-text);
  }

  .story-stat {
    color: var(--color-accent-amber);
    font-style: normal;
    font-weight: var(--weight-regular);
  }

  .drop-cap {
    float: left;
    font-family: var(--font-display);
    font-style: normal;
    font-size: 3.8em;
    line-height: 0.75;
    padding-right: 0.08em;
    padding-top: 0.06em;
    color: var(--color-accent-teal);
  }

  /* ---- Pull Quotes (centered, visually distinct) ---- */
  .pull-quote {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    line-height: 1.35;
    color: var(--color-text);
    text-align: center;
    margin: clamp(2rem, 4vw, 3rem) 0;
    padding: var(--space-element) clamp(1rem, 3vw, 2rem);
    border-top: 1px solid color-mix(in oklab, var(--color-accent) 25%, transparent);
    border-bottom: 1px solid color-mix(in oklab, var(--color-accent) 25%, transparent);
    position: relative;
  }

  /* ---- Chapter Dividers ---- */
  .chapter-divider {
    border: none;
    height: 1px;
    background: linear-gradient(90deg, transparent, color-mix(in oklab, var(--color-text-muted) 20%, transparent), transparent);
    margin: clamp(2.5rem, 5vw, 4rem) 0;
  }

  /* ---- Section Break (story → convictions) ---- */
  .section-break {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 0.75rem;
    padding: clamp(2rem, 5vw, 4rem) 0;
  }

  .break-dot {
    width: 4px;
    height: 4px;
    border-radius: 50%;
    background: color-mix(in oklab, var(--color-accent) 40%, transparent);
  }

  /* ---- Chapters ---- */
  .chapter {
    margin-bottom: 0;
  }

  .chapter-marker {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.6rem;
    margin-bottom: clamp(1.25rem, 2.5vw, 1.75rem);
  }

  .marker-year {
    font-family: var(--font-display);
    font-size: var(--text-md);
    line-height: 1;
    color: var(--color-accent);
  }

  .marker-dash {
    width: 20px;
    height: 1px;
    background: linear-gradient(90deg, var(--color-accent), transparent);
  }

  .marker-label {
    font-family: var(--font-body);
    font-size: var(--text-xs);
    font-weight: var(--weight-medium);
    letter-spacing: var(--tracking-wide);
    text-transform: uppercase;
    color: var(--color-text-muted);
  }

  /* ---- Photo treatments ---- */
  .story-img {
    filter: grayscale(0.15) contrast(1.02);
    transition: filter var(--duration-normal) var(--ease-out-expo);
  }

  figure:hover .story-img {
    filter: grayscale(0) contrast(1);
  }

  figure {
    margin: 0;
  }

  figcaption {
    font-family: var(--font-body);
    font-size: var(--text-sm);
    font-style: italic;
    color: var(--color-text-muted);
    margin-top: 0.5rem;
    line-height: 1.4;
    text-align: center;
  }

  /* Full-width photo */
  .photo-full {
    margin-bottom: 1.75rem;
  }

  .photo-full :global(img) {
    width: 100%;
    height: auto;
    display: block;
  }

  /* Paired photos side-by-side */
  .photo-pair {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    margin-bottom: 1.75rem;
  }

  .photo-pair :global(img) {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    aspect-ratio: 4 / 3;
  }

  /* Feature photo: centered, generous width */
  .photo-feature {
    max-width: 65%;
    margin: 0 auto 1.75rem;
  }

  .photo-feature :global(img) {
    width: 100%;
    height: auto;
    display: block;
  }

  /* Closer: the final photo, slightly smaller for intimacy */
  .photo-closer {
    max-width: 50%;
    margin-top: 1rem;
  }

  /* Wide photo: centered, large */
  .photo-wide {
    max-width: 85%;
    margin: 0 auto 1.75rem;
  }

  .photo-wide :global(img) {
    width: 100%;
    height: auto;
    display: block;
  }

  /* Small inline photo: centered */
  .photo-inline-small {
    max-width: 280px;
    margin: 0 auto 1.75rem;
  }

  .photo-inline-small :global(img) {
    width: 100%;
    height: auto;
    display: block;
  }

  /* ---- Convictions (centered headings, left body) ---- */
  .convictions {
    margin-bottom: var(--space-block-lg);
  }

  .convictions-heading {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: var(--weight-regular);
    line-height: 1.2;
    letter-spacing: var(--tracking-tight);
    color: var(--color-text);
    margin: 0 0 clamp(2rem, 4vw, 3rem);
    text-align: center;
  }

  .conviction-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-element);
  }

  .conviction {
    padding: var(--space-card);
    border: 1px solid color-mix(in oklab, var(--color-text-muted) 12%, transparent);
    border-top: 2px solid color-mix(in oklab, var(--color-accent-amber) 35%, transparent);
    transition: border-top-color var(--duration-fast) var(--ease-out-expo);
  }

  .conviction:hover { border-top-color: var(--color-accent-amber); }

  .conviction-number {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    line-height: 1;
    opacity: 0.12;
    display: block;
    margin-bottom: 0.75rem;
    color: var(--color-accent-amber);
  }

  .conviction-title {
    font-family: var(--font-display);
    font-size: var(--text-lg);
    font-weight: var(--weight-regular);
    line-height: 1.3;
    color: var(--color-text);
    margin: 0 0 0.5rem;
  }

  .conviction-text {
    font-family: var(--font-body);
    font-size: var(--text-base);
    line-height: 1.6;
    color: var(--color-text-muted);
    margin: 0;
  }

  /* ---- Skills (centered heading, 2x2 grid) ---- */
  .skills {
    margin-bottom: var(--space-block-lg);
  }

  .skills-heading {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: var(--weight-regular);
    line-height: 1.2;
    letter-spacing: var(--tracking-tight);
    color: var(--color-text);
    margin: 0 0 clamp(2rem, 4vw, 3rem);
    text-align: center;
  }

  .skills-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-element);
  }

  .skill-group {
    padding: var(--space-card);
    border: 1px solid color-mix(in oklab, var(--color-text-muted) 10%, transparent);
    border-top: 2px solid color-mix(in oklab, var(--color-accent-rust) 35%, transparent);
    transition: border-top-color var(--duration-fast) var(--ease-out-expo);
  }

  .skill-group:hover { border-top-color: var(--color-accent-rust); }

  .group-label {
    font-family: var(--font-display);
    font-size: var(--text-md);
    font-weight: var(--weight-regular);
    line-height: 1.25;
    letter-spacing: var(--tracking-tight);
    color: var(--color-text);
    margin: 0 0 0.75rem;
  }

  .group-desc {
    font-family: var(--font-body);
    font-size: var(--text-base);
    line-height: 1.55;
    color: var(--color-text-muted);
    margin: 0;
  }

  /* ---- CTA ---- */
  .closing-cta {
    text-align: center;
    padding: var(--space-block) 0;
  }

  .cta-text {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: var(--weight-regular);
    line-height: 1.3;
    color: var(--color-text-muted);
    margin: 0;
  }

  .cta-link {
    color: var(--color-accent);
    text-decoration: underline;
    text-underline-offset: 3px;
    text-decoration-color: color-mix(in oklab, var(--color-accent) 40%, transparent);
    transition: text-decoration-color var(--duration-fast) var(--ease-out-expo);
  }

  .cta-link:hover {
    text-decoration-color: var(--color-accent);
  }

  /* ---- Footer ---- */
  .page-footer {
    margin-top: var(--space-block-lg);
    padding-top: 2rem;
    border-top: 1px solid color-mix(in oklab, var(--color-text-muted) 15%, transparent);
    text-align: center;
  }

  .back-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-body);
    font-size: var(--text-sm);
    font-weight: var(--weight-regular);
    letter-spacing: var(--tracking-wide);
    text-transform: uppercase;
    color: var(--color-text-muted);
    text-decoration: none;
    transition: color var(--duration-fast) var(--ease-out-expo);
  }

  .back-link:hover {
    color: var(--color-accent);
  }

  .back-link svg {
    transition: transform var(--duration-fast) var(--ease-out-expo);
  }

  .back-link:hover svg {
    transform: translateX(-3px);
  }

  /* ---- Responsive ---- */
  @media (max-width: 768px) {
    .conviction-grid {
      grid-template-columns: 1fr;
    }

    .photo-feature {
      max-width: 100%;
    }

    .photo-closer {
      max-width: 70%;
    }

    .photo-wide {
      max-width: 100%;
    }

    .photo-inline-small {
      max-width: 60%;
    }

    .photo-pair {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 480px) {
    .skills-grid {
      grid-template-columns: 1fr;
    }

    .photo-inline-small {
      max-width: 70%;
    }

    .photo-closer {
      max-width: 80%;
    }
  }
</style>
